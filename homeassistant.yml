name:
  homeassistant
services:
  homeassistant:
    image:
      ghcr.io/home-assistant/home-assistant:stable
    container_name:
      homeassistant
    volumes:
      - config:/config
      - localtime:/etc/localtime:ro
      - dbus:/run/dbus:ro
    ports:
      - "8123:8123"
    network_mode:
      bridge
    restart:
      unless-stopped
    environment:
      TZ:
        Africa/Cairo
volumes:
  config:
  localtime:
  dbus:
